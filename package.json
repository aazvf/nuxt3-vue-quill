{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && aws s3 sync .output/public s3://nuxt3-vue-quill/ --profile s3deploy --delete"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.11",
    "nuxt": "3.0.0-rc.10",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "@vueup/vue-quill": "^1.0.0-beta.10",
    "localforage": "^1.10.0"
  }
}
